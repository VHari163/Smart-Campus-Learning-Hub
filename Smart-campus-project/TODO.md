# TODO - Smart Campus Learning Hub

- [ ] Seed predefined placeholder Notes into localStorage only when `scmh_notes` is empty (no real PDFs; show EX/Description only).
- [ ] Update Notes UI so placeholder notes do not break download flow (disable Download when no `pdfDataUrl`).
- [ ] Add “Quick templates” section on Notes page to speed up adding/filtering notes.
- [x] Improve Notes performance by memoizing derived values (e.g., memoized quick templates + filters) and reducing unnecessary computations.

- [ ] Test: run app, verify placeholders appear only once, verify user-uploaded PDFs persist and can be downloaded.

